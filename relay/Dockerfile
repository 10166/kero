FROM golang:1.27.1-alpine AS build
WORKDIR /src
COPY go.mod go.sum* ./
RUN go mod download
COPY . .
RUN CGO_ENABLED=0 go build -trimpath -ldflags='-s -w' -o /out/kero-relay ./cmd/kero-relay

FROM gcr.io/distroless/static-debian12:nonroot
COPY --from=build /out/kero-relay /kero-relay
VOLUME ["/data"]
EXPOSE 8080
ENTRYPOINT ["/kero-relay"]
