diff --git a/Package.swift b/Package.swift
index b32ee85..fbd4acb 100644
--- a/Package.swift
+++ b/Package.swift
@@ -45,8 +45,7 @@ let package = Package(
         ),
         .binaryTarget(
             name: "libghostty",
-            url: "https://github.com/egoist-labs/libghostty-spm/releases/download/storage.1.3.4/GhosttyKit.xcframework.zip",
-            checksum: "ea1f11d478007a8267db9a880c597c84d3c2d534eedc31287b5e5f58befe450f"
+            path: "BinaryTarget/GhosttyKit.xcframework"
         ),
         .testTarget(
             name: "GhosttyKitTest",
diff --git a/Script/apply-patches.sh b/Script/apply-patches.sh
index fc5077f..952e6ae 100755
--- a/Script/apply-patches.sh
+++ b/Script/apply-patches.sh
@@ -46,12 +46,12 @@ apply_unified_patch() {
         return
     fi
 
-    if patch -p1 -R --dry-run -d "$SOURCE_DIR" <"$patch_file" >/dev/null 2>&1; then
+    if patch --force -p1 -R --dry-run -d "$SOURCE_DIR" <"$patch_file" >/dev/null 2>&1; then
         echo "[+] patch already applied: $(basename "$patch_file")"
         return
     fi
 
-    if ! patch -p1 --dry-run -d "$SOURCE_DIR" <"$patch_file" >/dev/null 2>&1; then
+    if ! patch --force -p1 --dry-run -d "$SOURCE_DIR" <"$patch_file" >/dev/null 2>&1; then
         echo "[-] failed to validate patch: $patch_file"
         exit 1
     fi
diff --git a/Sources/GhosttyTerminal/Platform/AppKit/AppTerminalView+Input.swift b/Sources/GhosttyTerminal/Platform/AppKit/AppTerminalView+Input.swift
index 4648d10..ef6b31d 100644
--- a/Sources/GhosttyTerminal/Platform/AppKit/AppTerminalView+Input.swift
+++ b/Sources/GhosttyTerminal/Platform/AppKit/AppTerminalView+Input.swift
@@ -109,7 +109,7 @@
             _ = copySelectedTextToPasteboard()
         }
 
-        @IBAction func paste(_: Any?) {
+        @IBAction open func paste(_: Any?) {
             let pasteboard = NSPasteboard.general
             let text = TerminalPasteboard.string(from: pasteboard)
             if let text {
diff --git a/Patches/ghostty/0004-daemon-renderer-responses.patch b/Patches/ghostty/0004-daemon-renderer-responses.patch
new file mode 100644
--- /dev/null
+++ b/Patches/ghostty/0004-daemon-renderer-responses.patch
@@ -0,0 +1,86 @@
+--- a/src/config/Config.zig
++++ b/src/config/Config.zig
+@@ -2388,6 +2388,9 @@
+ ///   * `allow`
+ ///   * `deny`
+ ///
++/// Kero host-managed renderer: protocol responses are owned by the daemon.
++@"kero-renderer-only": bool = false,
++
+ @"clipboard-read": ClipboardAccess = .ask,
+ @"clipboard-write": ClipboardAccess = .allow,
+ 
+--- a/src/termio/Termio.zig
++++ b/src/termio/Termio.zig
+@@ -165,6 +165,7 @@
+     background: configpkg.Config.Color,
+     osc_color_report_format: configpkg.Config.OSCColorReportFormat,
+     clipboard_write: configpkg.ClipboardAccess,
++    renderer_only: bool,
+     enquiry_response: []const u8,
+     conditional_state: configpkg.ConditionalState,
+ 
+@@ -201,6 +202,7 @@
+             .background = config.background,
+             .osc_color_report_format = config.@"osc-color-report-format",
+             .clipboard_write = config.@"clipboard-write",
++            .renderer_only = config.@"kero-renderer-only",
+             .enquiry_response = try alloc.dupe(u8, config.@"enquiry-response"),
+             .conditional_state = config._conditional_state,
+ 
+@@ -285,6 +287,7 @@
+         .terminal = &self.terminal,
+         .osc_color_report_format = opts.config.osc_color_report_format,
+         .clipboard_write = opts.config.clipboard_write,
++        .renderer_only = opts.config.renderer_only,
+         .enquiry_response = opts.config.enquiry_response,
+         .default_cursor_style = opts.config.cursor_style,
+         .default_cursor_blink = opts.config.cursor_blink,
+@@ -510,6 +513,7 @@
+ }
+ 
+ fn sizeReportLocked(self: *Termio, td: *ThreadData, style: termio.Message.SizeReport) !void {
++    if (self.config.renderer_only) return;
+     const grid_size = self.size.grid();
+     const report_size: terminalpkg.size_report.Size = .{
+         .rows = grid_size.rows,
+@@ -709,6 +713,7 @@
+ }
+ 
+ pub fn colorSchemeReportLocked(self: *Termio, td: *ThreadData, force: bool) !void {
++    if (self.config.renderer_only) return;
+     if (!force and !self.renderer_state.terminal.modes.get(.report_color_scheme)) {
+         return;
+     }
+--- a/src/termio/stream_handler.zig
++++ b/src/termio/stream_handler.zig
+@@ -55,6 +55,7 @@
+ 
+     /// The clipboard write access configuration.
+     clipboard_write: configpkg.ClipboardAccess,
++    renderer_only: bool = false,
+ 
+     //---------------------------------------------------------------
+     // Internal state
+@@ -109,6 +110,7 @@
+     pub fn changeConfig(self: *StreamHandler, config: *termio.DerivedConfig) void {
+         self.osc_color_report_format = config.osc_color_report_format;
+         self.clipboard_write = config.clipboard_write;
++        self.renderer_only = config.renderer_only;
+         self.enquiry_response = config.enquiry_response;
+         self.default_cursor_style = config.cursor_style;
+         self.default_cursor_blink = config.cursor_blink;
+@@ -136,6 +138,13 @@
+     }
+ 
+     inline fn messageWriter(self: *StreamHandler, msg: termio.Message) void {
++        // Suppress responses at their source, not by guessing which callback
++        // bytes came from typing. User input travels through a different path.
++        if (self.renderer_only) switch (msg) {
++            .write_alloc => |v| { v.alloc.free(v.data); return; },
++            .write_small, .write_stable, .size_report, .color_scheme_report => return,
++            else => {},
++        };
+         self.termio_mailbox.send(msg, self.renderer_state.mutex);
+         self.termio_messaged = true;
+     }
diff --git a/Patches/ghostty/0005-metal-toolchain-environment.patch b/Patches/ghostty/0005-metal-toolchain-environment.patch
new file mode 100644
--- /dev/null
+++ b/Patches/ghostty/0005-metal-toolchain-environment.patch
@@ -0,0 +1,18 @@
+--- a/src/build/MetallibStep.zig
++++ b/src/build/MetallibStep.zig
+@@ -56,6 +56,7 @@
+         b.fmt("metal {s}", .{opts.name}),
+     );
+     run_ir.addArgs(&.{ "/usr/bin/xcrun", "-sdk", sdk, "metal", "-o" });
++    if (b.graph.env_map.get("KERO_METAL_DEVELOPER_DIR")) |developer| run_ir.setEnvironmentVariable("DEVELOPER_DIR", developer);
+     const output_ir = run_ir.addOutputFileArg(b.fmt("{s}.ir", .{opts.name}));
+     run_ir.addArgs(&.{"-c"});
+     for (opts.sources) |source| run_ir.addFileArg(source);
+@@ -71,6 +72,7 @@
+         b.fmt("metallib {s}", .{opts.name}),
+     );
+     run_lib.addArgs(&.{ "/usr/bin/xcrun", "-sdk", sdk, "metallib", "-o" });
++    if (b.graph.env_map.get("KERO_METAL_DEVELOPER_DIR")) |developer| run_lib.setEnvironmentVariable("DEVELOPER_DIR", developer);
+     const output_lib = run_lib.addOutputFileArg(b.fmt("{s}.metallib", .{opts.name}));
+     run_lib.addFileArg(output_ir);
+     run_lib.step.dependOn(&run_ir.step);
diff --git a/Patches/ghostty/0006-checkpoint-repeat-state.patch b/Patches/ghostty/0006-checkpoint-repeat-state.patch
new file mode 100644
--- /dev/null
+++ b/Patches/ghostty/0006-checkpoint-repeat-state.patch
@@ -0,0 +1,52 @@
+--- a/src/terminal/stream.zig
++++ b/src/terminal/stream.zig
+@@ -35,6 +35,7 @@
+     print: Print,
+     print_slice: PrintSlice,
+     print_repeat: usize,
++    kero_restore_preceding: u32,
+     bell,
+     backspace,
+     horizontal_tab: u16,
+@@ -134,6 +135,7 @@
+             "print",
+             "print_slice",
+             "print_repeat",
++            "kero_restore_preceding",
+             "bell",
+             "backspace",
+             "horizontal_tab",
+@@ -1557,6 +1559,13 @@
+                 },
+ 
+                 // Repeat Previous Char (REP)
++                'z' => {
++                    if (std.mem.eql(u8, input.intermediates, ">") and input.params.len == 3 and input.params[0] == 997) {
++                        const value: u32 = (@as(u32, input.params[1]) << 16) | @as(u32, input.params[2]);
++                        const preceding: u32 = if (value > 0 and value <= 0x10ffff and !(value >= 0xd800 and value <= 0xdfff)) value else 0;
++                        self.handler.vt(.kero_restore_preceding, preceding);
++                    }
++                },
+                 'b' => switch (input.intermediates.len) {
+                     0 => self.handler.vt(.print_repeat, switch (input.params.len) {
+                         0 => 1,
+--- a/src/terminal/stream_terminal.zig
++++ b/src/terminal/stream_terminal.zig
+@@ -141,6 +141,7 @@
+             .print => try self.terminal.print(value.cp),
+             .print_slice => try self.terminal.printSlice(value.cps),
+             .print_repeat => try self.terminal.printRepeat(value),
++            .kero_restore_preceding => self.terminal.previous_char = if (value > 0 and value <= 0x10ffff) @intCast(value) else null,
+             .backspace => self.terminal.backspace(),
+             .carriage_return => self.terminal.carriageReturn(),
+             .linefeed => try self.terminal.linefeed(),
+--- a/src/termio/stream_handler.zig
++++ b/src/termio/stream_handler.zig
+@@ -219,6 +219,7 @@
+                 try self.terminal.printSlice(value.cps);
+             },
+             .print_repeat => try self.terminal.printRepeat(value),
++            .kero_restore_preceding => self.terminal.previous_char = if (value > 0 and value <= 0x10ffff) @intCast(value) else null,
+             .bell => self.bell(),
+             .backspace => self.terminal.backspace(),
+             .horizontal_tab => self.horizontalTab(value),
diff --git a/Patches/ghostty/0007-kitty-cursor-position.patch b/Patches/ghostty/0007-kitty-cursor-position.patch
new file mode 100644
--- /dev/null
+++ b/Patches/ghostty/0007-kitty-cursor-position.patch
@@ -0,0 +1,13 @@
+--- a/src/terminal/kitty/graphics_exec.zig
++++ b/src/terminal/kitty/graphics_exec.zig
+@@ -274,7 +274,9 @@
+                 };
+ 
+                 terminal.setCursorPos(
+-                    terminal.screens.active.cursor.y,
++                    // setCursorPos uses 1-based coordinates. Preserve the row
++                    // reached by index() instead of moving back up one line.
++                    terminal.screens.active.cursor.y + 1,
+                     pin.x + size.cols + 1,
+                 );
+             },
