# Persistent-session verification — 2026-09-21

Implementation is integrated into the GUI. This record distinguishes exercised
behavior from builds and remaining acceptance coverage; it is not a release claim.

## Passed checks

- 16 daemon lifecycle/filesystem integration tests: same PID/env/cwd/jobs after
  detach, instance identity rejection, compatible reuse/protocol mismatch,
  raw output offsets, bounded frames, slow clients, explicit termination,
  file conflict protection, CRUD/watch/Git/repository operations, cold restart
  history/directory restoration without re-executing a task, relay checkpoint barrier.
  Fixtures include nonempty theme palettes in real Create requests and a burst
  of 500 single-byte key frames.
- Four daemon unit checks also pass, including attachment quota/expiry.
  New lifecycle cases cover wrong-host image rejection, private PNG bytes/SHA,
  authoritative bracketed paste, attachment removal on explicit termination,
  and reaping naturally exited detached shells.
- 12 canonical/shared graphics state tests, including every byte boundary in Chinese/escape
  sequences, two screens, modes, wrap/origin/margins/character sets, REP parser
  state after erasure, and history beyond output replay limits.
- Actual Ghostty + Alacritty renderer conformance for primary and alternate
  screens, Chinese/combining text, split CSI and UTF-8, REP, saved cursor,
  50,000 lines of output, synchronized updates, and parser reset on reattachment.
  Three additional image fixtures cover inline pixels, cursor movement and a
  split upload; Alacritty placements and PNG bytes are compared explicitly.
- Existing 32 Alacritty bridge tests, relay Go tests, credential refresh/401
  regression checks, and Ghostty remote-file-transfer filtering checks.
- Native localhost SSH: unknown fingerprint rejection, checksum rejection,
  repeated install, native ProxyJump, disconnect and same-shell reattachment.
- Authorized Ubuntu x86_64 SSH: native install, background job, same PID/env/cwd,
  Chinese alternate screen with split control sequence across transport loss,
  repository init/stage/commit/branch/status, bounded flood and checkpoint recovery.
- Actual Swift HostService against Linux: CRUD, SHA save conflict, rename, Git
  commit/branch/diff, invalidated capability rejection. A local file with the
  identical path spelling remained unchanged.
- GUI remote project: AppKit host creation, collapse/expand, same remote shell
  PID `1673762`, unsaved editor retained while disconnected, blocked remote save,
  reconnect/save, Git stage/commit (`76416db`, disposable verification repository).
- Latest GUI local test: PID `57234`, background sleep `57729`, environment and
  directory survived collapse, normal quit/relaunch, and GUI SIGKILL/relaunch.
  Chinese alternate-screen content survived. Explicit tab close removed both
  processes; closing while disconnected displayed a connect-first prompt.
- GUI window close/reopen kept PID `57233` and its environment. After killing
  only the disposable acceptance daemon, GUI showed the new-shell banner,
  restored `/tmp/kero-cold-gui-21` and history, and used PID `82174` with the old
  environment absent.
- Latest Alacritty GUI against Ubuntu: PID `1694008`, environment and job survived
  collapse/quit/relaunch. An unsaved remote draft survived quit with its host
  collapsed; save stayed blocked until expansion, then wrote the remote file.
  GUI SIGKILL/relaunch restored the Chinese alternate screen.
- All 14 lifecycle/filesystem checks executed against the macOS x86_64 asset
  under Rosetta. Linux arm64 asset ran under QEMU on the authorized Ubuntu host:
  detach/attach kept PID/env/cwd; explicit termination acknowledged. Its local
  Unix-socket reconnect was 4 ms, excluding SSH and emulation startup.
- Universal macOS Release app built (arm64 + x86_64); all four daemon target
  artifacts built. These are build checks, not four physical-machine tests.
- Final signed package's actual embedded macOS daemon passed all 14 lifecycle
  checks. Its four embedded installer assets matched their SHA-256 manifests
  after signing; the GUI executable contains both macOS architectures.
- The exact packaged SSH gateway passed cancellation during connection (2–11 ms)
  and after connection (10 ms), a burst of single-byte input frames, and verified
  same PID/env/cwd after reconnect against Ubuntu. PID `1704288` was then ended
  with an acknowledged close; the pre-existing session list was unchanged.
  The full gateway startup/install-check/reconnect path took 4,011 ms in this run;
  this includes more work than the transport-only measurement below.
- HostGroups source checks passed with two model windows: an instance change
  invalidates stale file capabilities, a repeated instance does not interrupt
  current operations, and synthetic workspace wake notifications exclude
  collapsed SSH groups. Connections are mocked; this is not a UI or real-sleep test.
- After the desktop was unlocked, actual final-package AppKit checks passed:
  Alacritty displayed Chinese alternate-screen text and an inline red image;
  collapse/reconnect retained their placement. Switching to Ghostty and reopening
  the app restored the same text/image and PID `1705040`, with its environment
  still present. GUI SIGKILL/relaunch preserved that alternate-screen image too.
- Two actual windows synchronized SSH group collapse: collapsing in the second
  window collapsed the first outline, showed its disconnected placeholder and
  removed the SSH gateway process.
- An actual Connecting-state collapse returned to the disconnected placeholder.
  Leaving the group collapsed kept the SSH gateway absent. Remote cold recovery
  restored the working directory, displayed the new-shell banner and resumed
  file-tree watching (`after-daemon-restart.txt` appeared without manual refresh).
- The final SSH recovery fix passed the native localhost test and real Ubuntu
  GUI acceptance. Updating the packaged helper preserved PID `1707743` and its
  environment. Killing only the isolated remote daemon while leaving SSH alive
  then recovered automatically, without a collapse/expand action: new PID
  `1708786`, restored `/tmp/kero-preview-gui-21` and history, explicit new-shell
  banner, old environment absent. `automatic-recovery-watch.txt` immediately
  appeared in the file tree; the three temporary watch fixtures were removed.
  The updated package's embedded helper again passed all 14 lifecycle checks.
- Explicit macOS clipboard copy from a real Ghostty SSH terminal to TextEdit
  preserved displayed Chinese and Emoji (`COPY_CHECK 中文 emoji🙂 tab     END`).
- With the image-capable isolated namespace, copying a 64×48 PNG in macOS
  Preview and Command-V in the SSH terminal uploaded a private remote PNG.
  The actual Codex 0.155.1 input UI displayed `[Image #1]`. Its model provider
  was an isolated loopback fixture; no prompt/model request was submitted.
- Actual Claude Code 2.1.278, launched in a real daemon PTY on Ubuntu with an
  isolated loopback provider, recognized the uploaded quoted image path as
  `[Image #1]` through bracketed paste. Raw PTY output confirmed attachment
  recognition; this second check did not depend on desktop automation and did
  not submit a model request.
- Private-pasteboard Swift checks passed for PNG/file-URL capture, paths with
  spaces, PNG dimensions, plain text fallback, and invalid/oversized images.
- After desktop access resumed, both actual renderers passed clipboard checks:
  Ghostty and Alacritty copied Chinese/Emoji text into TextEdit; both showed
  image attachments in Claude Code and Codex. Alacritty accepted Command-V and
  Control-V, retained Claude's three unsent attachments across collapse/reconnect,
  and did not create a fourth attachment during recovery.
- Pasting an image while collapsed displayed the connect-first alert, with no
  SSH gateway process running. Ending the three clipboard test tabs removed
  their shells/Agent processes and all uploaded images. The now-empty local and
  remote clipboard-test daemons were stopped; temporary CLI downloads and
  isolated provider configurations were removed.
- A reconnect redraw issue in Alacritty was corrected by invalidating cached GPU
  rows after a full checkpoint. Relaunch and background reconnect were exercised
  again with Claude's existing input preserved and visible without a key press.
- The exact final Release GUI was launched and its SSH project exercised. A PNG
  copied as a file in Finder pasted as a verified remote 64×48 PNG (mode 0600),
  without inserting a newline. Closing that tab removed its image; a fresh
  connected shell was left open for the user.

## Playable artifact

- Latest app: `/tmp/Kero Daemon Preview 2.app` (launched after building).
- Archive: `build/previews/Kero-Daemon-Preview.zip` (about 50 MiB).
- Archive SHA-256: `88c10d58700eb637dfff5fcec698cd003ea10990a2d77694488c324342bffc2d`.
- Bundle ID: `sh.kero.dev.daemon-preview2`; configuration, local daemon and remote
  daemon namespaces are all `kero-daemon-preview2`. The old Preview app and its
  tasks remain untouched. These parallel namespaces are test isolation, not
  normal reconnect behavior within one installed app.
- Both GUI and helper contain arm64/x86_64 slices. All four bundled installer
  assets matched their manifests after ad-hoc signing; strict signature
  verification passed. No release was published or version changed.
- Latest packaged helper passed all 16 lifecycle tests. Its real Ubuntu SSH
  gateway also drove the Claude Code image-attachment check above.
- Clipboard Agent GUI acceptance used the matching new Debug implementation
  under `kero-clipboard-acceptance`; the final Release additionally passed the
  Finder-image path above. The packaged helper passed another five-round Linux
  resource check (25 exits/five watchers, no repeated install).
- Re-signing this ad-hoc test app changed its Keychain access identity. Its
  signed-out, test-only relay device identity was recreated before the final
  launch. No production credential or SSH key was changed. This is not a
  notarized distribution build.

## Measurements

Native gateway resource churn on the authorized Ubuntu host (30 reconnects,
150 naturally exiting shells, 30 directory-watch subscriptions):

- Daemon PID remained `1722267`; one retained shell's PID and daemon instance
  stayed identical through every reconnect. No reconnect emitted Installing.
- With that shell retained, FD count stayed **8** and thread count **5**.
- RSS: **3,084 KiB** initially, **4,212 KiB** at rounds 5/10/15/20/25,
  **4,276 KiB** at round 30. This finite run showed a warm-up plateau; it does
  not establish unlimited-duration leak freedom.
- After ending the retained shell: **5 FDs**, **2 threads**, **4,312 KiB RSS**;
  session list and attachment cache were empty. Every local gateway exited and
  its temporary socket directory was removed. The isolated daemon was then
  stopped and its state removed; other namespaces were untouched.
- Sampled complete gateway connection times: **1,555–1,946 ms**. The preexisting
  checksum-addressed asset was verified and reused without upload.
- The reusable `tests/run-daemon-resource-checks.py` additionally passed a
  five-round run (25 exits, five watchers), including its guarded cleanup.

Single authorized Ubuntu x86_64 run with one PTY:

- Native SSH reconnect, checkpoint, and verified output: **966 ms** (earlier runs 767 ms and 1,026 ms).
- 4 MiB producer completed in **0.149 s**, **26.82 MiB/s** through the host PTY.
- The WAN client received 2,040,087 bytes before its bounded queue detached it;
  the job completed and reattachment recovered the final screen in 2,324 ms
  measured from flood start. This is not sustained WAN rendering throughput.
- Daemon RSS after output: **31,900 KiB**, not peak memory.
- Native localhost SSH reconnect and verified output: **102 ms** (earlier runs 70 ms and 120 ms).

## Coverage still requiring confirmation

- Physical Linux arm64 and Intel Mac execution. QEMU and Rosetta results above
  do not replace physical hardware testing.
- OS reboot and actual sleep/wake, extended multi-window UI stress, every authentication
  method and installer permission failure, and real two-device relay E2E.
- Shared graphics support excludes animations, Unicode placeholders, relative
  placements and file/shared-memory transfers; these are not claimed as restored.
- Desktop automation became available after the user unlocked the Mac. The
  AppKit, image and two-window checks listed above are now exercised results;
  a Connecting-state cancellation passed in the GUI as well as the gateway
  tests, but extended timed GUI stress is not claimed.

No version was changed and no release was published. Local preview builds are
ad-hoc signed, not notarized. Test fixtures leave immutable checksum-addressed
server binaries but clean their own UUID-scoped temporary daemons/repositories.
The isolated Preview GUI project remains available for hands-on use. User-created
remote projects and their original daemon remain untouched.
